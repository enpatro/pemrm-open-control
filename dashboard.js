import { KPI_MASTER,DEPTS,ABS_CALC,MONTHS } from './master-data.js';import{curMonth,setStatus,getAllDocsFB,toExcel,toPpt,configOk}from'./common.js';
const kpi=document.getElementById('kpiDashTable'),abs=document.getElementById('absDashTable'),cards=document.getElementById('summaryCards');function sid(s){return s.replace(/[^a-zA-Z0-9]/g,'_')}function mc(m){return m===curMonth()?'month-current':'month-other'}function td(m){return m===curMonth()?' month-current-cell':''}function num(x){return x!==''&&x!=null&&Number.isFinite(Number(x))}function fmt(x){if(x===''||x==null)return'';let n=Number(x);return Number.isFinite(n)?Math.round(n*100)/100:x}function st(v,t){if(!num(v)||!num(t))return['',''];v=Number(v);t=Number(t);if(t===0)return v===0?['OK','okcell']:['RED','redcell'];return(v>=t*.8&&v<=t*1.2)?['OK','okcell']:['RED','redcell']}function divVal(a,m,p){let vals=DEPTS.map(d=>Number((a[`${m}_${d}`]||{})[p.key])).filter(Number.isFinite);if(!vals.length)return'';let s=vals.reduce((x,y)=>x+y,0);return p.calc==='avg'?s/vals.length:s}function wrong(a,m,p){if(!num(p.target))return'';let out=[];for(const d of DEPTS){let doc=a[`${m}_${d}`]||{},v=doc[p.key];if(!num(v)){out.push(`${d}: missing${doc.remarks?` (${doc.remarks})`:''}`);continue}let rr=st(v,p.target);if(rr[0]==='RED')out.push(`${d}: ${fmt(v)} vs ${p.target}${doc.remarks?` (${doc.remarks})`:''}`)}return out.join(' | ')}
async function load(){try{let kd={},ad={};setStatus('Loading...');try{kd=await getAllDocsFB('kpis');ad=await getAllDocsFB('absot')}catch(e){setStatus('Firebase load error: '+e.message,'bad')}cards.innerHTML=`<div class='summary-card'><b>Current:</b> ${curMonth()}</div><div class='summary-card'><b>Auto:</b> Abs% and OT/person calculated from entry inputs</div><div class='summary-card'><b>Rule:</b> ±20% OK, beyond RED</div>`;let kh='<tr><th>Area</th><th>Parameter</th><th>Target</th>'+MONTHS.map(m=>`<th class='${mc(m)}'>${m}</th>`).join('')+'</tr>';for(const x of KPI_MASTER){kh+=`<tr><td class='area'>${x.area}</td><td class='param'>${x.parameter}</td><td>${x.monthTarget}</td>`;for(const m of MONTHS){let r=kd[`${m}_${sid(x.area+'_'+x.parameter)}`]||{};kh+=`<td class='${td(m)}'>${[r.actual??'',r.judgt??'',r.trend??''].filter(z=>z!==''&&z!=null).join(' ')}<br><small>${r.remarks??''}</small></td>`}kh+='</tr>'}kpi.innerHTML=kh;let ah='<tr><th>Section</th><th>Parameter</th><th>Target</th>'+MONTHS.map(m=>`<th class='${mc(m)}'>${m}</th>`).join('')+'<th>Combined Remark - Wrong Dept</th></tr>';let last='';for(const p of ABS_CALC){if(p.section!==last){ah+=`<tr class='section-title'><td colspan='${MONTHS.length+4}'>${p.section}</td></tr>`;last=p.section}ah+=`<tr><td>${p.section}</td><td class='param'>${p.param}</td><td>${p.target!==''?p.target:'-'}</td>`;let rem=[];for(const m of MONTHS){let v=divVal(ad,m,p),ss=st(v,p.target),w=wrong(ad,m,p);if(w)rem.push(`${m}: ${w}`);ah+=`<td class='${td(m)} ${ss[1]}'><b>${fmt(v)}</b>${ss[0]?`<br><small>${ss[0]}</small>`:''}${w?`<br><small>${w}</small>`:''}</td>`}ah+=`<td>${rem.join('<br>')}</td></tr>`}abs.innerHTML=ah;setStatus('Dashboard loaded','ok')}catch(e){setStatus('Dashboard script error: '+e.message,'bad');console.error(e)}}
refreshBtn.onclick=load;excelBtn.onclick=()=>toExcel('Dashboard.xls','Dashboard',kpi.outerHTML+abs.outerHTML);pptBtn.onclick=()=>toPpt('Dashboard.ppt','Dashboard',kpi.outerHTML+abs.outerHTML);setTimeout(load,400);
